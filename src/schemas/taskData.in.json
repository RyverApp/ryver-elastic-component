{
  "type": "object",
  "properties": {
    "tags": {
        "type": "string",
        "required": false,
        "title": "Tags (Optional)"
    },
    "checkList": {
        "type": "string",
        "required": false,
        "title": "Checklist (Optional)"
    },
    "idAssign": {
        "type": "string",
        "required": false,
        "title": "User ID for assign (Optional)"
    },
    "subject": {
        "type": "string",
        "required": true,
        "title": "Task subject"
    },
    "body": {
        "type": "string",
        "required": true,
        "title": "Task description"
    }
  }
}
